@model Actor

@{
	ViewData["Title"] = "Delete actor";
}

<div class="row text">
	<div class="col-md-8 offset-2">
		<p>
			<h1>Delete confirmation for @Model.FullName</h1>
		</p>
		<div class="row">
			<div class="col-md-8 offset-2">
					<div class="form-group text-center">
						<img id="ProfilePicturePreview" class="border-info rounded-circle" style="max-width: 150px" />
					</div>
					<div class="form-group">
						<label asp-for="ProfilePictureURL" class="control-label"></label>
						<input asp-for="ProfilePictureURL" class="form-control" readonly/>
						<span asp-validation-for="ProfilePictureURL" class="text-danger"></span>
					</div>
					<div class="form-group">
						<label asp-for="FullName" class="control-label"></label>
					<input asp-for="FullName" class="form-control" readonly />
						<span asp-validation-for="FullName" class="text-danger"></span>
					</div>
					<div class="form-group">
						<label asp-for="Bio" class="control-label"></label>
					<input asp-for="Bio" class="form-control" readonly />
						<span asp-validation-for="Bio" class="text-danger"></span>
					</div>
					<div class="form-group justify-content-between mt-2">
						<form asp-action="Delete" method="post">
							<input type="hidden" asp-for="Id" />
							<input type="submit" value="Confirm" class="btn btn-danger float-end"/>
						</form>
						<a class="btn btn-outline-secondary" asp-action="Index">Show All</a>
					</div>

			</div>
		</div>
	</div>
</div>

@section Scripts {
	<script>
		// $(document).ready(function () {
		// 	var outputContainer = document.getElementById('ProfilePictureContainer');
		// 	var img = '<img id="ProfilePicturePreview" style="width: 150px;" alt="Profile Picture Preview" src="' + $(this).val() + '" />';
		// 	outputContainer.innerHTML = img; // 插入img元素
		// })
		$(document).ready(function () {
			var output = document.getElementById('ProfilePicturePreview');
			output.src = $("#ProfilePictureURL").val();
		})

		


				// $("#ProfilePictureInput").on("change", function () {
				// 	var outputContainer = document.getElementById('ProfilePictureContainer');

				// 	// 检查或创建ProfilePictureContainer
				// 	// if (!outputContainer) {
				// 	// 	outputContainer = document.createElement('div');
				// 	// 	outputContainer.id = 'ProfilePictureContainer';
				// 	// 	outputContainer.className = 'form-group text-center';
				// 	// 	// 将新创建的容器插入到指定位置
				// 	// 	var container = document.querySelector('.form-group.text-center');
				// 	// 	container.appendChild(outputContainer);
				// 	// }

				// 	//outputContainer.innerHTML = ''; // 清空之前的内容

				// 	var img = '<img id="ProfilePicturePreview" style="width: 150px;" alt="Profile Picture Preview" src="' + $(this).val() + '" />';
				// 	outputContainer.innerHTML = img; // 插入img元素
				//});
	</script>
}